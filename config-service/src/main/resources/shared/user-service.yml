spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgresdb:5432/user_service
    username: admin
    password: admin
  data:
    jdbc:
      dialect: postgresql

server:
  port: 9090
