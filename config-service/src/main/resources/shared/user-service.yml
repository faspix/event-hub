spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://127.0.0.1:7744/user_service
    username: admin
    password: admin
  data:
    jdbc:
      dialect: postgresql

server:
  port: 9090
