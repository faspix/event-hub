spring:
  liquibase:
    enabled: false
  config:
    import: optional:configserver:http::/localhost:8001
  jpa:
    hibernate:
      ddl-auto: create
  elasticsearch:
    uris: http://localhost:9200


eureka:
  client:
    enabled: false
